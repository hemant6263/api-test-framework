name: Product list under load
env: qa

auth: { type: bearer }

request:
  method: GET
  path: /api/product

profile:
  vusers: 20
  duration: 60s
  rampUp: 10s
  rampDown: 5s

thresholds:
  maxErrorRate: 0.02
  maxP95Ms: 800
